apiVersion: v1
kind: Service
metadata:
  name: vulnerable-service
  namespace: security-test
spec:
  selector:
    app: vulnerable-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer