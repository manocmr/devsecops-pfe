# .trivy.yaml — à la racine du projet
severity:
  - CRITICAL
  - HIGH

exit-code: 1

vuln-type:
  - os
  - library

scanners:
  - vuln
  - misconfig
  - secret

# Seuil CVSS : bloquer si score >= 7.0
cvss-score-threshold: 7.0

format: table

ignorefile: .trivyignore
  ```

---

## 3. `.trivyignore` — ignorer des CVE connues/acceptées
```
# CVE ignorées volontairement (faux positifs ou acceptées)
# CVE-2023-XXXX